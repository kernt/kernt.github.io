---
tags:
  - monitoring
  - icingaweb2
  - icinga2
---
# icinga2 web API

**Abrufen von objektiven in Icinga2**

`icinga2 object list --type host --name host.example.com`

### icinga2 web Filter

`?host=localhost`
`icinga2 cli`

`icinga2 object list --type host --name $hostname`

`icinga2 object list --type service --name  $hostname`