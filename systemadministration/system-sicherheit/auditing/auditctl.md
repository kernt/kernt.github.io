
**Statistiken ausgeben**

```sh
auditctl -s
```

https://linux-audit.com/logging-root-actions-by-capturing-execve-system-calls/

**Definierte Rules ausgeben**

```sh
auditctl -l
```

**Refresh the audit rules from a file**

`auditctl -R /etc/audit/audit.rules`

